# Core GUI
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# Document Readers
PyMuPDF>=1.23.0          # PDF rendering & text extraction
EbookLib>=0.18           # EPUB parsing
python-docx>=1.1.0       # DOCX parsing
beautifulsoup4>=4.12.0   # HTML/XML parsing for EPUB
markdown>=3.5.0          # Markdown rendering

# Image Processing
Pillow>=10.0.0           # Cover image processing

# Utilities
natsort>=8.4.0           # Natural sorting for filenames

# Development
pytest>=7.4.0
pytest-qt>=4.2.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.1

# APIs e OPDS
httpx>=0.25.0
fastapi>=0.100.0
uvicorn>=0.23.00
ruff>=0.1.0

# Servidor MCP local (stdio) — expõe a biblioteca a hosts MCP (Claude etc.)
# Pin na série v1: a migração ao SDK v2 (spec 2026-07-28) é rodada própria.
mcp>=1.28,<2

# RAG / IA Local
chromadb>=0.4.0,<1.0    # Vector store local (sem servidor externo)
ollama>=0.2.0            # Python client para o daemon Ollama

# Leitor de Audio Local Offline (TTS em camadas)
kokoro>=0.9.4            # TTS neural padrão (usa GPU se disponível, senão CPU)
piper-tts>=1.4.2         # TTS de fallback (ONNX, CPU)
pyttsx3>=2.99            # TTS Engine legado offline local

# OCR de PDFs escaneados (RapidOCR via ONNX — sem binário de sistema, roda em CPU)
rapidocr-onnxruntime>=1.3.0

# Tradução Offline (Fase 8) + TTS neural (Kokoro)
# torch do PyPI = CPU. Para GPU NVIDIA (inclui Blackwell/sm_120, ex. RTX 5060 Ti),
# instale a build CUDA 12.8 a partir do índice do PyTorch (ver README, seção GPU):
#   pip install torch==2.11.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# Stack GPU validada congelada em reports/phase_13b3_gpu_lab_requirements.txt
torch>=2.0.0             # Tensor Backend para inferência local (CPU por padrão)
transformers>=4.33.0     # Suporte ao NLLB-200
sentencepiece>=0.1.99    # Tokenizer exigido pelo NLLB-200
